<h2> List of Contacts 
	<%if params[:company_id]%>
		for <%=@company.name%>
	<%end%>
	</h2>
		
	

<div class = 'row'>
	<div class = 'span2'>
		<b>Last Name</b>
	</div>
	
	<div class = 'span2'>
		<b>First Name</b>
	</div>
	
	<div class = 'span2'>
		<b>Phone </b>
	</div>
	
	<div class = 'span6'>
		<b>Comments </b>
	</div>
	
</div>

<%@contacts.each do |c|%>
<div class = 'row'>
	
	<div class = 'span2'>
		<%if params[:company_id]%>
			<%=link_to c.last_name, "/companies/#{@company.id}/contacts/#{c.id}"%>
		<%else%>
			<%=link_to c.last_name, "/contacts/#{c.id}"%>
		<%end%>
	</div>
	
	<div class = 'span2'>
		<%=c.first_name%>
	</div>
	
	<div class = 'span2'>
		<%=c.phone%></p>
	</div>
	
	<div class = 'span6'>
		<%=c.comments%></p>
	</div>

</div>


<%end%>

<%if params[:company_id]%>
	<%=link_to 'Add a new Contact', new_company_contact_path, :class => 'btn btn-mini btn-primary'%>
	<%=link_to 'Return to Company Info', company_path(@company), :class => 'btn btn-mini btn-primary'%>
<%else%>
	<%=link_to 'Add a new Contact', new_contact_path, :class => 'btn btn-mini btn-primary'%>
<%end%>
